apiVersion: v1
kind: Service
metadata:
  name: canary-tutorial-app-stable
  namespace: canary-tutorial
  labels:
    app: canary-tutorial-app
    role: stable
spec:
  selector:
    app: canary-tutorial-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: LoadBalancer
